#include <stdio.h>

/**
 * main - prints numbers base 10
 *
 * Return: Always return 0
 */
int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)
		printf("%d", num);
	printf("\n");

	return (0);
}
