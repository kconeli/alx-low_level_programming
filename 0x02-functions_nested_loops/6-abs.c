#include"main.h"

/**
 * _abs - outputs absolute value of integer input
 * @n: integer to check
 *
 * Return: absolute value of integer
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
		else
		return (-n);
}
